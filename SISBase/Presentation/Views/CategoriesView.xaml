<UserControl x:Class="SISBase.Presentation.Views.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource AppCardStyle}" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="170"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Buscar por ID, nombre o descripcion" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2" Command="{Binding SearchCommand}" Style="{StaticResource AppOutlineButtonStyle}" Content="Buscar"/>
                <Button Grid.Column="4" Command="{Binding NewCommand}" Style="{StaticResource AppPrimaryButtonStyle}" Content="Nueva Categoria"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource AppCardStyle}" Padding="16,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Lista de Categorias" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,14"/>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="220"/>
                        <DataGridTextColumn Header="Descripcion" Binding="{Binding Description}" Width="*"/>
                        <DataGridTemplateColumn Header="Acciones" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Width="30" Height="30" Margin="0,0,6,0" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="PencilOutline" Width="16" Height="16"/>
                                        </Button>
                                        <Button Width="30" Height="30" Command="{Binding DataContext.DeleteRowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="DeleteOutline" Width="16" Height="16" Foreground="#B91C1C"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <DockPanel Grid.Row="2" Margin="0,12,0,0">
                    <TextBlock Text="{Binding PaginationSummary}" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                        <Button Content="Primera" Width="68" Height="34" Margin="3,0" Style="{StaticResource AppOutlineButtonStyle}" Command="{Binding FirstPageCommand}"/>
                        <Button Content="Anterior" Width="76" Height="34" Margin="3,0" Style="{StaticResource AppOutlineButtonStyle}" Command="{Binding PreviousPageCommand}"/>
                        <Button Content="Siguiente" Width="76" Height="34" Margin="3,0" Style="{StaticResource AppOutlineButtonStyle}" Command="{Binding NextPageCommand}"/>
                        <Button Content="Ultima" Width="64" Height="34" Margin="3,0" Style="{StaticResource AppOutlineButtonStyle}" Command="{Binding LastPageCommand}"/>
                    </StackPanel>
                </DockPanel>
            </Grid>
        </Border>

        <Grid Background="#66000000" Visibility="{Binding IsFormOpen, Converter={StaticResource BoolToVisibilityConverter}}" Panel.ZIndex="10">
            <Border Style="{StaticResource AppCardStyle}" Width="500" Padding="22" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="Formulario de Categoria" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,14"/>
                    <TextBox IsReadOnly="True" Text="{Binding SelectedCategory.Id}" materialDesign:HintAssist.Hint="ID"/>
                    <TextBox Text="{Binding SelectedCategory.Name, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Nombre de categoria"/>
                    <TextBox Text="{Binding SelectedCategory.Description, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Descripcion" AcceptsReturn="True" MinHeight="80"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                        <Button Command="{Binding CloseFormCommand}" Style="{StaticResource AppOutlineButtonStyle}" Width="110" Margin="0,0,8,0" Content="Cancelar"/>
                        <Button Command="{Binding SaveCommand}" Style="{StaticResource AppPrimaryButtonStyle}" Width="110" Content="Guardar"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
